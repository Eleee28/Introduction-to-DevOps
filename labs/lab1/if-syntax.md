while <cmd>; do
	...
done

<cmd> can be any command -> if exit code 0 = true

[ ] define a program

[ is a file found in path, it is an executable

alternative program is test (same functionality as [)

if test == if [ ]
